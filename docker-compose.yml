version: '2'

services:
  resume:
    build: .
    container_name: resume
    ports:
      - 5001:5001
    volumes:
      - ./resume.json:/resume.json
      - ./resume.md.jinja2:/resume.md.jinja2
    restart: always
    networks:
      - resume_net

networks:
  resume_net:
    driver: bridge
